rabbitmq:
  expose:
    - "4369"
    - "5672"
    - "15672"
    - "25672"
  image: rabbitmq
  container_name: rabbitmq
  hostname: rabbitmq

tester:
  image: ${IMAGE}
  links:
    - rabbitmq
  working_dir: /src
  volumes:
    - ${PWD}:/src
  environment:
    NODE_ENV: ${NODE_ENV}
